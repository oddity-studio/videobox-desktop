node_modules/
.next/
out/
tsconfig.tsbuildinfo
*.log
.DS_Store
# public/ excluded from the render image. Asset files (videos, audio,
# images, Lottie transition JSONs) are served from the CDN. The render
# server fetches transition JSONs once on startup (see
# loadTransitionPreloads in server.mjs) and proxies everything else
# through the /cache route (singleflight + on-disk caching).
public/
