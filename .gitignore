node_modules/
out/
.next/
tsconfig.tsbuildinfo
*.log
.DS_Store
public/
.env
.env.*
!.env.example
!deploy/.env.example
# GCS service account keys live here (e.g. env/audeobox-sounds-signer.json)
# — was previously unprotected since only .env/.env.* were ignored, not
# this directory itself.
env/
